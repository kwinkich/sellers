// Practice participation roles (used inside practice flows)
export type PracticeParticipantRole = "SELLER" | "BUYER" | "MODERATOR" | "OBSERVER";

// Authenticated user account roles (used for app navigation and permissions)
export type AuthUserRole = "ADMIN" | "CLIENT" | "MOP";
